# WARNING: This file is machine generated by fidlgen.

from __future__ import annotations

from dataclasses import dataclass
from fidl_codec import add_ir_path, encode_fidl_object
import sys
import typing
import enum

import fidl
from fidl._ir import get_fidl_ir_map

_ir_path = get_fidl_ir_map()["test.zxtypes"]
add_ir_path(_ir_path)

from fidl._client import EventHandlerBase, FidlClient  # type: ignore[unused-ignore,unused-import]
import fidl._construct  # type: ignore[unused-ignore,unused-import]
from fidl._server import ServerBase  # type: ignore[unused-ignore,unused-import]
from fidl._fidl_common import DomainError, FrameworkError, MethodInfo, FidlProtocolMarker, normalize_identifier  # type: ignore[unused-ignore,unused-import]

from abc import abstractmethod, ABC  # type: ignore[unused-ignore,unused-import]
import collections.abc  # type: ignore[unused-ignore,unused-import]


@dataclass
class StatusProtocolARequest:
    arg: int

    __fidl_kind__ = "struct"
    __fidl_type__ = "StatusProtocolARequest"
    __fidl_raw_type__ = "test.zxtypes/StatusProtocolARequest"

    # TODO(https://fxbug.dev/394421154): We should probably remove this method when we
    # start making breaking changes.
    def __getitem__(self, item: str):  # type: ignore
        if not isinstance(item, str):
            raise TypeError("Subscripted item must be a string")
        return getattr(self, item)

    # TODO(https://fxbug.dev/394421154): We should probably return a more readable type.
    def encode(self) -> tuple[bytes, list[tuple[int, int, int, int, int]]]:
        return encode_fidl_object(
            self, "test.zxtypes", "test.zxtypes/StatusProtocolARequest"
        )

    # TODO(https://fxbug.dev/394421154): Assigning None (incorrectly) to each type is a consequence
    # of needing to support creation of a "default object" before decoding.
    @classmethod
    def make_default(cls) -> typing.Self:
        return cls(
            **{
                "arg": None,  # type: ignore[arg-type,unused-ignore]
            }
        )


@dataclass
class StatusProtocolBRequest:
    arg: int

    __fidl_kind__ = "struct"
    __fidl_type__ = "StatusProtocolBRequest"
    __fidl_raw_type__ = "test.zxtypes/StatusProtocolBRequest"

    # TODO(https://fxbug.dev/394421154): We should probably remove this method when we
    # start making breaking changes.
    def __getitem__(self, item: str):  # type: ignore
        if not isinstance(item, str):
            raise TypeError("Subscripted item must be a string")
        return getattr(self, item)

    # TODO(https://fxbug.dev/394421154): We should probably return a more readable type.
    def encode(self) -> tuple[bytes, list[tuple[int, int, int, int, int]]]:
        return encode_fidl_object(
            self, "test.zxtypes", "test.zxtypes/StatusProtocolBRequest"
        )

    # TODO(https://fxbug.dev/394421154): Assigning None (incorrectly) to each type is a consequence
    # of needing to support creation of a "default object" before decoding.
    @classmethod
    def make_default(cls) -> typing.Self:
        return cls(
            **{
                "arg": None,  # type: ignore[arg-type,unused-ignore]
            }
        )


@dataclass
class StatusProtocolAResponse:
    result: int

    __fidl_kind__ = "struct"
    __fidl_type__ = "StatusProtocolAResponse"
    __fidl_raw_type__ = "test.zxtypes/StatusProtocol_A_Response"

    # TODO(https://fxbug.dev/394421154): We should probably remove this method when we
    # start making breaking changes.
    def __getitem__(self, item: str):  # type: ignore
        if not isinstance(item, str):
            raise TypeError("Subscripted item must be a string")
        return getattr(self, item)

    # TODO(https://fxbug.dev/394421154): We should probably return a more readable type.
    def encode(self) -> tuple[bytes, list[tuple[int, int, int, int, int]]]:
        return encode_fidl_object(
            self, "test.zxtypes", "test.zxtypes/StatusProtocol_A_Response"
        )

    # TODO(https://fxbug.dev/394421154): Assigning None (incorrectly) to each type is a consequence
    # of needing to support creation of a "default object" before decoding.
    @classmethod
    def make_default(cls) -> typing.Self:
        return cls(
            **{
                "result": None,  # type: ignore[arg-type,unused-ignore]
            }
        )


@dataclass
class StatusProtocolBResponse:
    result: int

    __fidl_kind__ = "struct"
    __fidl_type__ = "StatusProtocolBResponse"
    __fidl_raw_type__ = "test.zxtypes/StatusProtocol_B_Response"

    # TODO(https://fxbug.dev/394421154): We should probably remove this method when we
    # start making breaking changes.
    def __getitem__(self, item: str):  # type: ignore
        if not isinstance(item, str):
            raise TypeError("Subscripted item must be a string")
        return getattr(self, item)

    # TODO(https://fxbug.dev/394421154): We should probably return a more readable type.
    def encode(self) -> tuple[bytes, list[tuple[int, int, int, int, int]]]:
        return encode_fidl_object(
            self, "test.zxtypes", "test.zxtypes/StatusProtocol_B_Response"
        )

    # TODO(https://fxbug.dev/394421154): Assigning None (incorrectly) to each type is a consequence
    # of needing to support creation of a "default object" before decoding.
    @classmethod
    def make_default(cls) -> typing.Self:
        return cls(
            **{
                "result": None,  # type: ignore[arg-type,unused-ignore]
            }
        )


@dataclass
class StatusStruct:
    status: int
    status_array: typing.Sequence[int]
    status_vector: typing.Sequence[int]

    __fidl_kind__ = "struct"
    __fidl_type__ = "StatusStruct"
    __fidl_raw_type__ = "test.zxtypes/StatusStruct"

    # TODO(https://fxbug.dev/394421154): We should probably remove this method when we
    # start making breaking changes.
    def __getitem__(self, item: str):  # type: ignore
        if not isinstance(item, str):
            raise TypeError("Subscripted item must be a string")
        return getattr(self, item)

    # TODO(https://fxbug.dev/394421154): We should probably return a more readable type.
    def encode(self) -> tuple[bytes, list[tuple[int, int, int, int, int]]]:
        return encode_fidl_object(
            self, "test.zxtypes", "test.zxtypes/StatusStruct"
        )

    # TODO(https://fxbug.dev/394421154): Assigning None (incorrectly) to each type is a consequence
    # of needing to support creation of a "default object" before decoding.
    @classmethod
    def make_default(cls) -> typing.Self:
        return cls(
            **{
                "status": None,  # type: ignore[arg-type,unused-ignore]
                "status_array": None,  # type: ignore[arg-type,unused-ignore]
                "status_vector": None,  # type: ignore[arg-type,unused-ignore]
            }
        )


@dataclass
class StatusStructAlias:
    status: int
    status_array: typing.Sequence[int]
    status_vector: typing.Sequence[int]

    __fidl_kind__ = "struct"
    __fidl_type__ = "StatusStructAlias"
    __fidl_raw_type__ = "test.zxtypes/StatusStructAlias"

    # TODO(https://fxbug.dev/394421154): We should probably remove this method when we
    # start making breaking changes.
    def __getitem__(self, item: str):  # type: ignore
        if not isinstance(item, str):
            raise TypeError("Subscripted item must be a string")
        return getattr(self, item)

    # TODO(https://fxbug.dev/394421154): We should probably return a more readable type.
    def encode(self) -> tuple[bytes, list[tuple[int, int, int, int, int]]]:
        return encode_fidl_object(
            self, "test.zxtypes", "test.zxtypes/StatusStructAlias"
        )

    # TODO(https://fxbug.dev/394421154): Assigning None (incorrectly) to each type is a consequence
    # of needing to support creation of a "default object" before decoding.
    @classmethod
    def make_default(cls) -> typing.Self:
        return cls(
            **{
                "status": None,  # type: ignore[arg-type,unused-ignore]
                "status_array": None,  # type: ignore[arg-type,unused-ignore]
                "status_vector": None,  # type: ignore[arg-type,unused-ignore]
            }
        )


@dataclass
class StatusTable:
    status: int | None
    status_array: typing.Sequence[int] | None
    status_vector: typing.Sequence[int] | None

    def __init__(
        self,
        status: int | None = None,
        status_array: typing.Sequence[int] | None = None,
        status_vector: typing.Sequence[int] | None = None,
    ) -> None:
        self.status = status
        self.status_array = status_array
        self.status_vector = status_vector

    __fidl_kind__ = "table"
    __fidl_type__ = "StatusTable"
    __fidl_raw_type__ = "test.zxtypes/StatusTable"

    # TODO(https://fxbug.dev/394421154): We should probably remove this method when we
    # start making breaking changes.
    def __getitem__(self, item: str):  # type: ignore
        if not isinstance(item, str):
            raise TypeError("Subscripted item must be a string")
        return getattr(self, item)

    # TODO(https://fxbug.dev/394421154): We should probably return a more readable type.
    def encode(self) -> tuple[bytes, list[tuple[int, int, int, int, int]]]:
        return encode_fidl_object(
            self, "test.zxtypes", "test.zxtypes/StatusTable"
        )

    @classmethod
    def make_default(cls) -> typing.Self:
        return cls()


@dataclass
class StatusTableAlias:
    status: int | None
    status_array: typing.Sequence[int] | None
    status_vector: typing.Sequence[int] | None

    def __init__(
        self,
        status: int | None = None,
        status_array: typing.Sequence[int] | None = None,
        status_vector: typing.Sequence[int] | None = None,
    ) -> None:
        self.status = status
        self.status_array = status_array
        self.status_vector = status_vector

    __fidl_kind__ = "table"
    __fidl_type__ = "StatusTableAlias"
    __fidl_raw_type__ = "test.zxtypes/StatusTableAlias"

    # TODO(https://fxbug.dev/394421154): We should probably remove this method when we
    # start making breaking changes.
    def __getitem__(self, item: str):  # type: ignore
        if not isinstance(item, str):
            raise TypeError("Subscripted item must be a string")
        return getattr(self, item)

    # TODO(https://fxbug.dev/394421154): We should probably return a more readable type.
    def encode(self) -> tuple[bytes, list[tuple[int, int, int, int, int]]]:
        return encode_fidl_object(
            self, "test.zxtypes", "test.zxtypes/StatusTableAlias"
        )

    @classmethod
    def make_default(cls) -> typing.Self:
        return cls()


class StatusProtocolAResult:
    _response: typing.Optional[StatusProtocolAResponse]
    _err: typing.Optional[int]
    _framework_err: typing.Optional[fidl.FrameworkError]

    __fidl_kind__ = "union"
    __fidl_type__ = "StatusProtocolAResult"
    __fidl_raw_type__ = "test.zxtypes/StatusProtocol_A_Result"
    _is_result = True

    def __eq__(self, other: object) -> bool:
        if not isinstance(other, type(self)):
            return False
        if self.response != other.response:
            return False
        if self.err != other.err:
            return False
        if self.framework_err != other.framework_err:
            return False
        return True

    def __repr__(self) -> str:
        """Returns the union repr in the format <'foo.bar.baz/FooUnion' object({value})>

        If {value} is not set, will write None."""
        variant = ""
        if self.response:
            variant = f"response={self.response!r}"
        if self.err:
            variant = f"err={self.err!r}"
        if self.framework_err:
            variant = f"framework_err={self.framework_err!r}"
        return f"<'{self.__fidl_type__}' object({variant})>"

    def __init__(
        self,
        response: typing.Optional[StatusProtocolAResponse] = None,
        err: typing.Optional[int] = None,
        framework_err: typing.Optional[fidl.FrameworkError] = None,
        _empty: typing.Optional[tuple[()]] = None,
    ):
        object.__init__(self)
        if _empty is not None:
            return

        number_of_variants = 0
        variants = []

        if response is not None:
            self._response = response
            variants.append("response")
            number_of_variants += 1
        if err is not None:
            self._err = err
            variants.append("err")
            number_of_variants += 1
        if framework_err is not None:
            self._framework_err = framework_err
            variants.append("framework_err")
            number_of_variants += 1
        if number_of_variants > 1:
            raise TypeError(
                f"Exactly one variant must be specified for {self.__fidl_raw_type__}: {variants}"
            )

    @property
    def response(self) -> StatusProtocolAResponse | None:
        return getattr(self, "_response", None)

    @property
    def err(self) -> int | None:
        return getattr(self, "_err", None)

    @property
    def framework_err(self) -> fidl.FrameworkError | None:
        return getattr(self, "_framework_err", None)

    # TODO(https://fxbug.dev/394421154): We should probably remove this method when we
    # start making breaking changes.
    def __getitem__(self, item: str):  # type: ignore
        if not isinstance(item, str):
            raise TypeError("Subscripted item must be a string")
        return getattr(self, item)

    # TODO(https://fxbug.dev/394421154): We should probably return a more readable type.
    def encode(self) -> tuple[bytes, list[tuple[int, int, int, int, int]]]:
        return encode_fidl_object(
            self, "test.zxtypes", "test.zxtypes/StatusProtocol_A_Result"
        )

    def unwrap(self) -> StatusProtocolAResponse:
        """Returns the response if result does not contain an error. Otherwise, raises an exception."""
        try:
            if (
                hasattr(self, "_framework_err")
                and self._framework_err is not None
            ):
                raise AssertionError(
                    f"{self.__fidl_raw_type__} framework error {self._framework_err}"
                )
        except AttributeError:
            pass
        try:
            if hasattr(self, "_err") and self._err is not None:
                raise AssertionError(
                    f"{self.__fidl_raw_type__} error {self._err}"
                )
        except AttributeError:
            pass

        assert (
            self._response is not None
        ), f"Failed to unwrap {self.__fidl_raw_type__}. Result does not contain an error or response."
        return self._response

    @classmethod
    def make_default(cls) -> typing.Self:
        return cls(_empty=())


class StatusProtocolBResult:
    _response: typing.Optional[StatusProtocolBResponse]
    _err: typing.Optional[int]
    _framework_err: typing.Optional[fidl.FrameworkError]

    __fidl_kind__ = "union"
    __fidl_type__ = "StatusProtocolBResult"
    __fidl_raw_type__ = "test.zxtypes/StatusProtocol_B_Result"
    _is_result = True

    def __eq__(self, other: object) -> bool:
        if not isinstance(other, type(self)):
            return False
        if self.response != other.response:
            return False
        if self.err != other.err:
            return False
        if self.framework_err != other.framework_err:
            return False
        return True

    def __repr__(self) -> str:
        """Returns the union repr in the format <'foo.bar.baz/FooUnion' object({value})>

        If {value} is not set, will write None."""
        variant = ""
        if self.response:
            variant = f"response={self.response!r}"
        if self.err:
            variant = f"err={self.err!r}"
        if self.framework_err:
            variant = f"framework_err={self.framework_err!r}"
        return f"<'{self.__fidl_type__}' object({variant})>"

    def __init__(
        self,
        response: typing.Optional[StatusProtocolBResponse] = None,
        err: typing.Optional[int] = None,
        framework_err: typing.Optional[fidl.FrameworkError] = None,
        _empty: typing.Optional[tuple[()]] = None,
    ):
        object.__init__(self)
        if _empty is not None:
            return

        number_of_variants = 0
        variants = []

        if response is not None:
            self._response = response
            variants.append("response")
            number_of_variants += 1
        if err is not None:
            self._err = err
            variants.append("err")
            number_of_variants += 1
        if framework_err is not None:
            self._framework_err = framework_err
            variants.append("framework_err")
            number_of_variants += 1
        if number_of_variants > 1:
            raise TypeError(
                f"Exactly one variant must be specified for {self.__fidl_raw_type__}: {variants}"
            )

    @property
    def response(self) -> StatusProtocolBResponse | None:
        return getattr(self, "_response", None)

    @property
    def err(self) -> int | None:
        return getattr(self, "_err", None)

    @property
    def framework_err(self) -> fidl.FrameworkError | None:
        return getattr(self, "_framework_err", None)

    # TODO(https://fxbug.dev/394421154): We should probably remove this method when we
    # start making breaking changes.
    def __getitem__(self, item: str):  # type: ignore
        if not isinstance(item, str):
            raise TypeError("Subscripted item must be a string")
        return getattr(self, item)

    # TODO(https://fxbug.dev/394421154): We should probably return a more readable type.
    def encode(self) -> tuple[bytes, list[tuple[int, int, int, int, int]]]:
        return encode_fidl_object(
            self, "test.zxtypes", "test.zxtypes/StatusProtocol_B_Result"
        )

    def unwrap(self) -> StatusProtocolBResponse:
        """Returns the response if result does not contain an error. Otherwise, raises an exception."""
        try:
            if (
                hasattr(self, "_framework_err")
                and self._framework_err is not None
            ):
                raise AssertionError(
                    f"{self.__fidl_raw_type__} framework error {self._framework_err}"
                )
        except AttributeError:
            pass
        try:
            if hasattr(self, "_err") and self._err is not None:
                raise AssertionError(
                    f"{self.__fidl_raw_type__} error {self._err}"
                )
        except AttributeError:
            pass

        assert (
            self._response is not None
        ), f"Failed to unwrap {self.__fidl_raw_type__}. Result does not contain an error or response."
        return self._response

    @classmethod
    def make_default(cls) -> typing.Self:
        return cls(_empty=())


class StatusUnion:
    _status: typing.Optional[int]
    _status_array: typing.Optional[typing.Sequence[int]]
    _status_vector: typing.Optional[typing.Sequence[int]]

    __fidl_kind__ = "union"
    __fidl_type__ = "StatusUnion"
    __fidl_raw_type__ = "test.zxtypes/StatusUnion"
    _is_result = False

    def __eq__(self, other: object) -> bool:
        if not isinstance(other, type(self)):
            return False
        if self.status != other.status:
            return False
        if self.status_array != other.status_array:
            return False
        if self.status_vector != other.status_vector:
            return False
        return True

    def __repr__(self) -> str:
        """Returns the union repr in the format <'foo.bar.baz/FooUnion' object({value})>

        If {value} is not set, will write None."""
        variant = ""
        if self.status:
            variant = f"status={self.status!r}"
        if self.status_array:
            variant = f"status_array={self.status_array!r}"
        if self.status_vector:
            variant = f"status_vector={self.status_vector!r}"
        return f"<'{self.__fidl_type__}' object({variant})>"

    def __init__(
        self,
        status: typing.Optional[int] = None,
        status_array: typing.Optional[typing.Sequence[int]] = None,
        status_vector: typing.Optional[typing.Sequence[int]] = None,
        _empty: typing.Optional[tuple[()]] = None,
    ):
        object.__init__(self)
        if _empty is not None:
            return

        number_of_variants = 0
        variants = []

        if status is not None:
            self._status = status
            variants.append("status")
            number_of_variants += 1
        if status_array is not None:
            self._status_array = status_array
            variants.append("status_array")
            number_of_variants += 1
        if status_vector is not None:
            self._status_vector = status_vector
            variants.append("status_vector")
            number_of_variants += 1
        if number_of_variants != 1:
            raise TypeError(
                f"Exactly one variant must be specified for {self.__fidl_raw_type__}: {variants}"
            )

    @property
    def status(self) -> int | None:
        return getattr(self, "_status", None)

    @property
    def status_array(self) -> typing.Sequence[int] | None:
        return getattr(self, "_status_array", None)

    @property
    def status_vector(self) -> typing.Sequence[int] | None:
        return getattr(self, "_status_vector", None)

    # TODO(https://fxbug.dev/394421154): We should probably remove this method when we
    # start making breaking changes.
    def __getitem__(self, item: str):  # type: ignore
        if not isinstance(item, str):
            raise TypeError("Subscripted item must be a string")
        return getattr(self, item)

    # TODO(https://fxbug.dev/394421154): We should probably return a more readable type.
    def encode(self) -> tuple[bytes, list[tuple[int, int, int, int, int]]]:
        return encode_fidl_object(
            self, "test.zxtypes", "test.zxtypes/StatusUnion"
        )

    @classmethod
    def make_default(cls) -> typing.Self:
        return cls(_empty=())


class StatusUnionAlias:
    _status: typing.Optional[int]
    _status_array: typing.Optional[typing.Sequence[int]]
    _status_vector: typing.Optional[typing.Sequence[int]]

    __fidl_kind__ = "union"
    __fidl_type__ = "StatusUnionAlias"
    __fidl_raw_type__ = "test.zxtypes/StatusUnionAlias"
    _is_result = False

    def __eq__(self, other: object) -> bool:
        if not isinstance(other, type(self)):
            return False
        if self.status != other.status:
            return False
        if self.status_array != other.status_array:
            return False
        if self.status_vector != other.status_vector:
            return False
        return True

    def __repr__(self) -> str:
        """Returns the union repr in the format <'foo.bar.baz/FooUnion' object({value})>

        If {value} is not set, will write None."""
        variant = ""
        if self.status:
            variant = f"status={self.status!r}"
        if self.status_array:
            variant = f"status_array={self.status_array!r}"
        if self.status_vector:
            variant = f"status_vector={self.status_vector!r}"
        return f"<'{self.__fidl_type__}' object({variant})>"

    def __init__(
        self,
        status: typing.Optional[int] = None,
        status_array: typing.Optional[typing.Sequence[int]] = None,
        status_vector: typing.Optional[typing.Sequence[int]] = None,
        _empty: typing.Optional[tuple[()]] = None,
    ):
        object.__init__(self)
        if _empty is not None:
            return

        number_of_variants = 0
        variants = []

        if status is not None:
            self._status = status
            variants.append("status")
            number_of_variants += 1
        if status_array is not None:
            self._status_array = status_array
            variants.append("status_array")
            number_of_variants += 1
        if status_vector is not None:
            self._status_vector = status_vector
            variants.append("status_vector")
            number_of_variants += 1
        if number_of_variants != 1:
            raise TypeError(
                f"Exactly one variant must be specified for {self.__fidl_raw_type__}: {variants}"
            )

    @property
    def status(self) -> int | None:
        return getattr(self, "_status", None)

    @property
    def status_array(self) -> typing.Sequence[int] | None:
        return getattr(self, "_status_array", None)

    @property
    def status_vector(self) -> typing.Sequence[int] | None:
        return getattr(self, "_status_vector", None)

    # TODO(https://fxbug.dev/394421154): We should probably remove this method when we
    # start making breaking changes.
    def __getitem__(self, item: str):  # type: ignore
        if not isinstance(item, str):
            raise TypeError("Subscripted item must be a string")
        return getattr(self, item)

    # TODO(https://fxbug.dev/394421154): We should probably return a more readable type.
    def encode(self) -> tuple[bytes, list[tuple[int, int, int, int, int]]]:
        return encode_fidl_object(
            self, "test.zxtypes", "test.zxtypes/StatusUnionAlias"
        )

    @classmethod
    def make_default(cls) -> typing.Self:
        return cls(_empty=())


StatusEnum = enum.IntEnum(
    "StatusEnum",
    {
        "__fidl_kind__": "enum",
        "__fidl_type__": "StatusEnum",
        "__fidl_raw_type__": "test.zxtypes/StatusEnum",
        "__strict__": False,
        "OK": 0,
        "make_default": classmethod(lambda cls: cls(0)),
    },
)


StatusEnumAlias = enum.IntEnum(
    "StatusEnumAlias",
    {
        "__fidl_kind__": "enum",
        "__fidl_type__": "StatusEnumAlias",
        "__fidl_raw_type__": "test.zxtypes/StatusEnumAlias",
        "__strict__": False,
        "OK": 0,
        "make_default": classmethod(lambda cls: cls(0)),
    },
)


# Generated aliases
StatusAlias = int


class StatusProtocolServer(ServerBase, ABC):
    __fidl_kind__ = "server"
    library = "test.zxtypes"
    method_map: typing.Dict[int, MethodInfo] = {
        910477041687900864: MethodInfo(
            name="a",
            request_ident="test.zxtypes/StatusProtocolARequest",
            requires_response=True,
            empty_response=False,
            has_result=True,
            response_identifier="test.zxtypes/StatusProtocol_A_Result",
        ),
        1940401666492920560: MethodInfo(
            name="b",
            request_ident="test.zxtypes/StatusProtocolBRequest",
            requires_response=True,
            empty_response=False,
            has_result=True,
            response_identifier="test.zxtypes/StatusProtocol_B_Result",
        ),
    }

    @staticmethod
    def construct_response_object(
        response_ident: str, response_obj: typing.Any
    ) -> typing.Any:
        return fidl._construct.construct_response_object(
            response_ident, response_obj
        )

    AResponse: typing.TypeAlias = typing.Union[
        FrameworkError,
        DomainError,
        StatusProtocolAResponse,
    ]

    @abstractmethod
    def a(
        self,
        request: StatusProtocolARequest,
    ) -> (
        collections.abc.Coroutine[typing.Any, typing.Any, AResponse] | AResponse
    ):
        ...

    BResponse: typing.TypeAlias = typing.Union[
        FrameworkError,
        DomainError,
        StatusProtocolBResponse,
    ]

    @abstractmethod
    def b(
        self,
        request: StatusProtocolBRequest,
    ) -> (
        collections.abc.Coroutine[typing.Any, typing.Any, BResponse] | BResponse
    ):
        ...


class StatusProtocolClient(FidlClient):
    __fidl_kind__ = "client"
    library = "test.zxtypes"

    @staticmethod
    def construct_response_object(
        response_ident: str, response_obj: typing.Any
    ) -> typing.Any:
        return fidl._construct.construct_response_object(
            response_ident, response_obj
        )

    def a(
        self,
        *,
        arg: int,
    ) -> collections.abc.Coroutine[
        typing.Any, typing.Any, StatusProtocolAResult
    ]:
        msg = StatusProtocolARequest(
            arg,
        )
        ret = typing.cast(
            collections.abc.Coroutine[
                typing.Any, typing.Any, StatusProtocolAResult
            ],
            self._send_two_way_fidl_request(
                910477041687900864,
                "test.zxtypes",
                msg,
                normalize_identifier("test.zxtypes/StatusProtocol_A_Result"),
            ),
        )
        return ret

    def b(
        self,
        *,
        arg: int,
    ) -> collections.abc.Coroutine[
        typing.Any, typing.Any, StatusProtocolBResult
    ]:
        msg = StatusProtocolBRequest(
            arg,
        )
        ret = typing.cast(
            collections.abc.Coroutine[
                typing.Any, typing.Any, StatusProtocolBResult
            ],
            self._send_two_way_fidl_request(
                1940401666492920560,
                "test.zxtypes",
                msg,
                normalize_identifier("test.zxtypes/StatusProtocol_B_Result"),
            ),
        )
        return ret


class StatusProtocolEventHandler(EventHandlerBase, ABC):
    __fidl_kind__ = "event_handler"
    library = "test.zxtypes"
    method_map: typing.Dict[int, MethodInfo] = {}

    @staticmethod
    def construct_response_object(
        response_ident: str, response_obj: typing.Any
    ) -> typing.Any:
        return fidl._construct.construct_response_object(
            response_ident, response_obj
        )


StatusProtocolMarker = FidlProtocolMarker(
    "(nondiscoverable) test.zxtypes/StatusProtocol"
)
